name: WhiteFly CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  # Frontend Build & Test
  frontend:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Build frontend
        working-directory: ./frontend
        run: npm run build

      - name: Upload frontend build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: frontend/dist/

  # Backend Test
  backend:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install backend dependencies
        working-directory: ./backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Django migrations
        working-directory: ./backend
        run: |
          python manage.py migrate

      - name: Run Django tests (if any)
        working-directory: ./backend
        run: |
          python manage.py test

  # Deploy to Vercel (Frontend) - Vercel auto-deploys from GitHub
  deploy-frontend:
    needs: [frontend, backend]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Vercel Auto-Deploy
        run: |
          echo "✅ Frontend build completed successfully"
          echo "🚀 Vercel will auto-deploy from this GitHub push"
          echo "📱 Frontend will be available at: https://cicd-whitefly-2ee8.vercel.app"

  # Deploy Backend to Render (or Railway)
  deploy-backend:
    needs: [frontend, backend]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Render Auto-Deploy
        run: |
          echo "✅ Backend tests completed successfully"
          echo "🚀 Render will auto-deploy from this GitHub push"
          echo "🔗 Backend API will be available at: https://cicd-whitefly-v2.onrender.com"
